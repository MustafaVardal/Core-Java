
public class CreditManager { // Business layer!
	public void Calculate(int creditType) {
		if (creditType == 1) { // esnaf
			
		}
		
		if (creditType == 2) { // ogretmen
			
		}
		
		// ortak metodlarin imzasini barindirir. bu interface in tanimi fakat gercek hayatta tanimi bu degil....
		// Buradaki islemler silsilsi ni kullamamamiz icin bize interface yapisi sunulmustur.
		
	}

	public void Loan() {
		System.out.println("Loan approval");

	}

	
	// Interface!!! Interface ler business layerlar arasinda gerceklesir.
	// is yapan siniflarin operasyonlarini sadece imza seviyesinde yazarak.
	// yazilimda bagimliligi korumak adina yapilan calismadir.
	
	
	// Bir projede ne kadar if varsa nesne yonelimli programlada o kadar zayif oldugundandir. 
}
